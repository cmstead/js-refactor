{
    "namedMultilineArrowFunction": [
        "const {name} = ({args}) => {",
        "{body}",
        "};"
    ],
    "multilineArrowFunction": [
        "({args}) => {",
        "{body}",
        "};"
    ],
    "namedSingleLineArrowFunction": [
        "const {name} = ({args}) => {body}"
    ],
    "singleLineArrowFunction": [
        "({args}) => {body}"
    ]
}