'use strict';

const value = 'something';

const obj = {
    foo: 'bar',
    num: 5
}

const astr = 'this is a test ' + value + ' and here is an object reference: ' + obj.foo + ' and some more stuff';

console.log(3 + 4 - obj.num);