const filesystem = require('fs');
const path = require('path');
const vscodeFactory = require('../../mock-modules/vscodeFactory');

function buildAstFixture(fixtureName) {

}

module.exports = buildAstFixture