'use strict';

const container = require('../../container');

describe('astHelper', function() {
    
    let astHelper;

    beforeEach(function() {
        astHelper = container.build('astHelper');
    });

    describe('isNodeType', function() {
        
        it('should perform action on node type match', function () {
            throw new Error('You left off where the error is thrown. Don\'t forget!');
        });

        it('should not perform action on node type mismatch');

    });

});